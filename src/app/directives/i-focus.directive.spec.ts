import { IFocusDirective } from './i-focus.directive';

describe('IFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new IFocusDirective();
    expect(directive).toBeTruthy();
  });
});
